import { playerStat } from "../types/playerStat";
import { webMsg } from "../types/webMsg";


export function handleTournament(playerInfos: playerStat, msg: webMsg) {
	if (msg.action === "Create") {

	}
	else if (msg.value  === "Join")
}